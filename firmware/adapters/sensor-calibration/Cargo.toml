[package]
name = "swp-sensor-calibration"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
swp-dsp-kernel = { path = "../../../infrastructure/dsp-kernel" }
swp-mpu6050 = { path = "../../sensors/mpu6050" }
swp-plant-observation = { path = "../../../plant/plant-observation" }

[lib]
path = "src/lib.rs"
