[package]
name = "swp-frame-transform"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
swp-dsp-kernel = { path = "../../infrastructure/dsp-kernel" }
swp-plant-observation = { path = "../../plant/plant-observation" }
swp-sensor-calibration = { path = "../sensor-calibration" }

[lib]
path = "src/lib.rs"
