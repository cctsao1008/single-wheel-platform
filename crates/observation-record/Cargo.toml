[package]
name = "swp-observation-record"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
swp-plant-observation = { path = "../plant-observation" }

[lib]
path = "src/lib.rs"
