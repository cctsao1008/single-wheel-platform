[package]
name = "swp-state-estimator"
version = "0.1.0"
description = "Canonical estimator contract and fixed-gain reference observer for the single-wheel platform"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
swp-dsp-kernel = { path = "../dsp-kernel" }
swp-plant-model = { path = "../plant-model" }
swp-measurement-model = { path = "../measurement-model" }
swp-robot-domain = { path = "../robot-domain" }

[lib]
path = "src/lib.rs"
