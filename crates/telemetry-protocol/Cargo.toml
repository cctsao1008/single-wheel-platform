[package]
name = "swp-telemetry-protocol"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
swp-plant-observation = { path = "../plant-observation" }

[lib]
path = "src/lib.rs"
