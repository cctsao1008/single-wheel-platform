[package]
name = "swp-dsp-kernel"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[target.'cfg(target_arch = "arm")'.dependencies]
cmsis_dsp = "0.2.0"

[lib]
path = "src/lib.rs"
